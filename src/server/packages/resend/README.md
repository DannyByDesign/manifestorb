# Email updates

This package is used to send transactional emails to users.

## Running locally

To preview email templates locally:

```bash
bun --cwd src/server/packages/resend run dev
```

Then visit `http://localhost:3010/` to view previews.
