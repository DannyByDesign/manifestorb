# Groups (`src/server/features/groups`)

Grouping and classification for senders/threads (for example: "Newsletters", "Receipts", "HR", etc.).

## Layout

- `ai/`: LLM helpers for group matching and suggestions
- `group-item.ts`: group item primitives
- `find-matching-group.ts`: deterministic/group matching logic

