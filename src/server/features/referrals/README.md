# Referrals (`src/server/features/referrals`)

Referral system feature module.

This directory currently contains no implementation files; the schema/models exist in Prisma and the feature is implemented elsewhere or is pending consolidation into this module.

