# Follow-Up (`src/server/features/follow-up`)

Follow-up reminders and auto-draft helpers for threads that are awaiting replies or need user action.

## Key Areas

- label helpers (`labels.ts`) for marking/organizing follow-up state
- draft generation (`generate-draft.ts`) for nudges/follow-ups
- cleanup logic (`cleanup.ts`) for maintaining follow-up labels/state

