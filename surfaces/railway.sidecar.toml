[build]
builder = "RAILPACK"
buildCommand = "echo \"no build step for sidecar\""

[deploy]
startCommand = "bun run start"
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 120
